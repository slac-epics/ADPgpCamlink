# loDevRogue.template
record( longout, "$(P)$(R)$(NAME)" )
{
	field( DESC, "$(DESC=?)" )
	field( DTYP, "RogueLO" )
	field( DOL,  "$(DOL=0)" )
	field( OUT,  "@B$(B) L$(L) $(PATH)" )
	field( PINI, "$(PINI=NO)" )
	info( autosaveFields, "$(SAVE=VAL)" )
}
